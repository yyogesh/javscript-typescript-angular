import { FallbackImagePipe } from './fallback-image.pipe';

describe('FallbackImagePipe', () => {
  it('create an instance', () => {
    const pipe = new FallbackImagePipe();
    expect(pipe).toBeTruthy();
  });
});
