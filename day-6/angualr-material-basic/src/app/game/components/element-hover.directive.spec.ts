import { ElementHoverDirective } from './element-hover.directive';

describe('ElementHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementHoverDirective();
    expect(directive).toBeTruthy();
  });
});
